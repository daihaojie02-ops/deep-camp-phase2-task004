# 复盘 — deep_patrol

## 为什么选这个痛点

选 DEEP 营任务状态巡检作为自动化目标，三个理由：

1. **真实重复：** 本周手动检查了至少 4 次，每次 2-5 分钟，痛点真实可量化
2. **数据可获取：** DEEP 营有 REST API（/api/grades），不需要爬 DOM，比页面抓取稳定得多
3. **产出有价值：** 不仅我自己能用，任何一个 DEEP 营学员拿过去替换 Cookie 就能用

如果选其他痛点（比如截图整理、文件改名），可能看起来更"通用"，但实际上我本周真正的重复操作就是反复打开 DEEP 营平台看状态。

## AI 帮了什么 / 没帮什么

**帮了的：**
- 快速生成初始代码框架（argparse + requests + JSON 解析）
- API 端点确认——我一开始想爬 DOM，AI 建议直接调 API，省了很多事
- 终端输出格式化——ASCII 表格和进度条的对齐逻辑

**没帮的（我的判断）：**
- Windows GBK 编码问题——AI 第一次生成的代码没有考虑 Windows 终端编码，我加了 `sys.stdout.reconfigure(encoding='utf-8')`
- Cookie 过期处理——AI 没提 401 错误场景，我在 fetch_json 里加了
- 告警生成逻辑——什么算"告警"（<75 分、未提交）是我自己定义的

## 哪一步本来不该用 AI

第一轮代码生成时，AI 同时生成了 `fetch_json`、`generate_report`、`terminal_report`、`markdown_report` 四个函数。如果让我重新来，我会分两步：
1. 先让 AI 只生成 `fetch_json` + `generate_report`，我自己验证数据结构
2. 确认数据结构正确后，再让 AI 生成终端输出和 Markdown 输出

这样可以避免"AI 一次生成太多→数据结构假设错误→改了好几个函数"的情况。Plan Mode 先行，确实更稳。

## 工具是否值得长期保留

**值得保留。**

理由：
- DEEP 营至少还有 4 期（第三期到结营考核），每周巡检省 14 分钟，总计至少省 1 小时
- 可以扩展——加个 cron 每天自动跑，输出到 GitHub 做进度追踪
- 迁移成本低——换个 Cookie 就能给其他 DEEP 营学员用

**局限性：**
- 依赖 DEEP 营 API 不变化（如果改接口需更新）
- Cookie 过期需手动刷新（加了提示但无法自动续）

## 诚实自评

我选的痛点确实重复了 4 次，pain_log 每条都是真实的（对话历史可查）。工具约 180 行，符合 ≤200 行的要求。Claude Code 实质性参与了架构设计和代码生成，不是直接复制粘贴。

如果重做，我会在第一步就用 `--cookie-file` 而不是 `--cookie`（命令行传 Cookie 有长度和编码问题），但这个改动不影响功能，下次迭代再加。
